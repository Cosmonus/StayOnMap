// Fetches a list of properties (for sidebar list view)
// Depends on filterStore for active filters

export function useProperties(_filters) {
  // TODO: fetch from property.service.getList(filters)
  // TODO: return { properties, isLoading, error, total }
}
