// Full property detail panel / page
// Props: id (fetches data internally via useProperty)
// Sections: images, overview, amenities, nearby, contact

export default function PropertyDetail({ id: _id }) {
  // TODO: const { property, isLoading } = useProperty(id)
  return null
}
