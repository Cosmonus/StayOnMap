// Single property marker on the map
// Props: property (id, lat, lng, rent, isSelected)
// On click: set mapStore.selectedPin

export default function PropertyPin({ property: _property, onClick: _onClick }) {
  // TODO: render Mapbox Marker with rent label
  // TODO: active state styling when isSelected
  return null
}
