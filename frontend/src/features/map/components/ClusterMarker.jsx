// Cluster bubble showing count of grouped pins
// Props: count, lat, lng, onClick

export default function ClusterMarker({ count: _count, onClick: _onClick }) {
  // TODO: render circular marker with count
  // TODO: on click, zoom in to expand cluster
  return null
}
