import PlaceholderScreen from '@/components/common/PlaceholderScreen'

export default function SavedScreen() {
  return <PlaceholderScreen title="Saved" subtitle="Saved homes land in a later build phase." />
}
